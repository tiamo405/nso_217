# Hướng dẫn tạo commit

- Viết commit bằng tiếng Việt có dấu theo mẫu: `<type>(<scope>): <icon> <mô tả>`.
- Giữ `type` và `scope` bằng tiếng Anh; chỉ thêm `scope` khi xác định rõ phạm vi thay đổi.
- Mô tả ngắn gọn, tối đa 50 ký tự, nêu đúng mục đích thay đổi và không kết thúc bằng dấu chấm.
- Chỉ thêm phần nội dung sau một dòng trống khi cần giải thích lý do hoặc chi tiết quan trọng.
- Không tự tạo mã issue. Nếu có issue liên quan, thêm `Fixes #<số>` hoặc `Closes #<số>` ở cuối.
- Với thay đổi không tương thích ngược, thêm `!` sau type/scope và dòng `BREAKING CHANGE: <mô tả>`.
- Chỉ trả về nội dung commit, không kèm Markdown hoặc lời giải thích.

## Type và biểu tượng

- `feat` ✨ tính năng mới
- `fix` 🐛 sửa lỗi
- `docs` 📝 tài liệu
- `style` 💄 định dạng mã nguồn
- `refactor` ♻️ tái cấu trúc mã
- `perf` ⚡ cải thiện hiệu năng
- `test` ✅ kiểm thử
- `build` 🏗️ hệ thống build hoặc dependency
- `ci` 👷 cấu hình CI/CD
- `chore` 🔧 công việc bảo trì
- `revert` ⏪ hoàn tác thay đổi

## Ví dụ

```text
feat(auth): ✨ thêm đăng nhập bằng JWT

Bổ sung xác thực JWT để bảo vệ các API riêng tư.

Fixes #123
```

```text
refactor(api)!: ♻️ chuẩn hóa endpoint REST

BREAKING CHANGE: các client phải sử dụng endpoint mới.
```
